# PostgreSQL 配置
POSTGRES_DB=law_oa_db
POSTGRES_USER=law_oa_user
POSTGRES_PASSWORD=law_oa_password_2024
POSTGRES_PORT=5432

# Elasticsearch 配置
ELASTICSEARCH_PORT=9200
KIBANA_PORT=5601

# Redis 配置
REDIS_PASSWORD=redis_password_2024
REDIS_PORT=6379

# Supabase 配置 (生产环境)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 数据库连接字符串
# 开发环境 (本地PostgreSQL)
DEV_DATABASE_URL=postgres://law_oa_user:law_oa_password_2024@localhost:5432/law_oa_db?sslmode=disable

# 生产环境 (Supabase)
PROD_DATABASE_URL=

# 应用配置
APP_ENV=development
LOG_LEVEL=debug