# 本轮浏览器探索问题台账

- 验证日期：2026-07-29
- 环境：本地前端 `http://127.0.0.1:3003`，真实页面交互，未直接调用接口，未修改数据库
- 账号范围：律师 A 的本地 QA 账号；数据为既有虚构演练数据

## BUG-20260729-001

- 严重度：P2，可用性
- 把握度：98%
- 状态：已修复并完成浏览器回归
- 复现路径：登录 → 工作台 → 查看“利益冲突待复核”卡片
- 预期：用户能直接看出入口用于查看冲突任务
- 实际：按钮无障碍名称为“查看全部冲突任务”，但可见文字只有“查看全部”
- 修复：统一可见文字和无障碍名称为“查看全部冲突任务”
- 回归证据：刷新 `/dashboard` 后 DOM 显示唯一按钮“查看全部冲突任务”，点击后进入 `/conflict`；控制台无 error/warn

## BUG-20260729-002

- 严重度：P1，流程语义
- 把握度：95%
- 状态：已修复并完成浏览器回归
- 复现路径：利益冲突清单 → 查看详情 → 查看已有“信息不足”人工复核记录
- 预期：律师能区分“复核已完成”和“案件仍被阻断”，并进入已有审批处置
- 实际：详情显示已有复核结论“信息不足”，同时接案文案仍称“当前命中尚未形成最终结论”，按钮仍称“发起冲突审批”；审批中心实际已有该冲突审批记录
- 修复：将状态文案改为“信息不足，暂停接案”，说明独立复核已完成但不得放行；已有复核记录时按钮改为“进入冲突审批”，并改写后续指引
- 回归证据：同一检测记录详情显示“信息不足，暂停接案”“独立复核已完成，但当前结论仍不足以放行接案”和“进入冲突审批”；控制台无 error/warn

## BUG-20260729-003

- 严重度：P1，跨模块一致性
- 把握度：98%
- 状态：已修复并完成浏览器回归
- 复现路径：案件管理 → 点击“冲突复核”筛选
- 预期：工作台统计为冲突复核中的案件时，案件应出现在该筛选结果
- 实际：案件 `QA-P0-A-2026-001` 的冲突任务存在，案件总列表显示“待处理”，工作台显示“冲突复核中 1”，但筛选结果显示“暂无案件”
- 修复：案件筛选在保留原状态映射的同时，使用当前已加载的可见冲突任务按案件 ID/编号/标题关联补足“冲突复核”结果；不改变后端状态和权限范围
- 回归证据：刷新 `/case` 后点击“冲突复核”，案件 `QA-P0-A-2026-001` 可见；进入案件详情再点击“进入本案冲突复核”可正确进入带案件上下文的 `/conflict`；控制台无 error/warn

## 其他探索结果

- 冲突筛选“高风险”在 0 条结果时显示明确空状态，没有控制台错误。
- 冲突详情、审批中心、待办中心、案件详情和客户档案各 Tab 均可加载，没有观察到新的浏览器 error/warn。
- 本轮没有把创建审批、提交复核、成案、上传、删除等会修改数据库的按钮作为测试动作。
